import { OrdemNumbersPipe } from './ordem-numbers.pipe';

describe('OrdemNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdemNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
